#include <stdio.h>

int main()
{
        int a,b;
        printf("Enter two integers separated by a space: ");
        scanf("%d%d",&a,&b);
        a=a+b;
        b=a-b;
        a=a-b;
        printf("The swapped integers are: %d,%d",a,b);
        return 0;
}
